export const checkBalance = (e) => {
  e.preventDefault();
  console.log(`checkBalance`);
};

export const withdraw = (e) => {
  e.preventDefault();
  console.log(`withdraw`);
};

export const fundme = (e) => {
  e.preventDefault();
  console.log(`fundme`);
};
